


apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: a0528075188-dev
  name: 'dataretrieval'
  annotations: {}
spec:
  selector:
    matchLabels:
      app: 'dataretrieval'
  replicas: 1
  template:
    metadata:
      labels:
        app: 'dataretrieval'
    spec:
      containers:
        - name: dataretrieval-container
          image: >-
            shmuelblau/dataretrieval:2
          ports:
            - containerPort: 8000
              protocol: TCP
          env: 
          
           

      imagePullSecrets: []
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  paused: false
